/**
 * Meteo L-Drago uses the legacy `meteo_ldrago.glb` from the initial BeyWeb import
 * (crimson / black rubber spin-steal ring). Do not overwrite it with the Lightning
 * recolor pipeline — run `npm run color-lightning-ldrago` for Lightning L-Drago instead.
 */
console.log('Meteo L-Drago model is meteo_ldrago.glb (restored legacy asset).');
console.log('To rebuild Lightning L-Drago, run: npm run color-lightning-ldrago');
